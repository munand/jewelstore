<p id="notice"><%= notice %></p>
<h2>Your NewEra Jewelry Cart</h2>
<ul>
<% @cart.line_items.each do |item| %>
<li><%= item.quantity %> &times; <%= item.product.name %></li>
<% end %>
</ul>

<%= button_to 'Empty cart', @cart, :method => :delete, :confirm => 'Are you sure?' %>

